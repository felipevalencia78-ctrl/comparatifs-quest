import React from "react";
import ReactDOM from "react-dom/client";
import ComparatifsApp from "./ComparatifsApp";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ComparatifsApp />
  </React.StrictMode>
);
